- clone the repository to CADE machine.
- First of all, make sure that you are in the correct directory which contains all the files in the DecisionTree folder.
- MLTEST.py file is the one where all my id3 and helper functions are written
- To make the 'run.sh' file executable use the command 'chmod u+x run.sh'
- To run the whole code, use the command './run.sh'
- Code will start printing the output for each question with header like '--------Question 2b--------' to the terminal
  
